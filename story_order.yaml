# Auto-generated by `indexer fetch` from the master-DB event release dates.
# Do NOT hand-edit: re-running the fetcher rewrites this file with all events
# in chronological (release) order, grouped by story_type. Use
# `part_order_overrides` only for hand-authored content the fetcher can't order.
#
# This checked-in stub is a placeholder so the config loads before the first
# fetch. It is replaced wholesale once `indexer fetch` runs.
chronological_order:
  - story_type: Main
    arcs: ["placeholder"]
summary_order:
  - story_type: Main
    arcs: ["placeholder"]
part_order_overrides: []
