{
  "_comment": "event_id -> focus character id (0 = force-exclude). Only for events where the banner ARTWORK character differs from the story protagonist (the commissioned-song rule already excludes mixed/collab events, so those don't need entries). See docs/focus_event_heuristic.md.",
  "97": 10
}
